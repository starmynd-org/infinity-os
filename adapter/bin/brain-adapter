#!/usr/bin/env python3
"""Entry point for the three verbs D2 owns. See brain_adapter/cli.py."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from brain_adapter.cli import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
