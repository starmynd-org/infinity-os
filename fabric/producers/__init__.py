"""Producers. Two of them, and no more.

The fabric's value is proven by one working path, not by six half-wired ones. n8n subscriptions,
metric anomalies, competitor research and intake are later subscribers against a proven contract,
and every one of them will call the same `event emit` these two do.

**Every producer calls the verb. None of them INSERTs.** That is what keeps the `producer` role's
INSERT-only grant meaningful rather than decorative: one implementation to audit, one place the
hard-flag check runs, one place retention and the 4096-byte ceiling are enforced.
"""
