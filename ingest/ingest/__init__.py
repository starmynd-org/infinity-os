"""D3: session registration and the transcript index.

The lane owns five verbs (verbs.py), a pure transcript reader (transcript.py), a goal
adjudicator that refuses to fabricate (goal.py) and the fabric's first producer (events.py).
`transcript` stores a pointer and a hash, never the blob.
"""
